defaults:
  - _self_
  - data: segthor.yaml
  - model: unetr.yaml
  - loss: dice_cce.yaml
  - optim: radam.yaml
  - sched: onecycle.yaml
  - metrics: default.yaml
  - hydra: default.yaml

device: cuda
epoch: 100
fp16: true

# seed for random number generators in pytorch, numpy and python.random
seed: 12345
