defaults:
  - _self_
  - data: segthor.yaml
  - model: unetr.yaml
  - loss: dice_cce.yaml
  - optim: adamw.yaml
  - sched: onecycle.yaml
  - metrics: default.yaml
  - hydra: default.yaml

device: cuda:0
epoch: 100
fp16: true

# seed for random number generators in pytorch, numpy and python.random
seed: null
